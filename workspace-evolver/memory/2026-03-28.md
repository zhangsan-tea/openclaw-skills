# 2026-03-28

## 🔴 Lee 核心铁律（最高优先级）

### 可逆性原则
**来源：** Lee 于 2026-03-28 00:11 明确要求
**原文：** "不是不能执行，而是你对我系统所有的改动必须是可逆的，如果是不可逆的，你要提出严重警告。"

**执行规则：**
1. 所有系统改动**必须可逆**
2. 执行前评估可逆性：
   - ✅ 可逆操作 → 正常执行，记录回滚方法
   - ⚠️ 不可逆操作 → **严重警告** + 等待 Lee 明确确认后才能执行
3. 不可逆操作包括但不限于：删除文件/数据、覆盖无备份内容、修改不可回退的系统配置
4. 执行改动前备份原始状态

---

**日志生成时间：** 01:18
**状态：** 🟢 已记录

---

## 🐛 问题诊断：弗洛一德无响应

**时间：** 2026-03-28 01:18 - 01:51

**症状：** 弗洛一德 Bot (@Freud2bot) 不响应消息

**排查过程：**
1. Token 验证 → 全部正常（之前检测脚本有编码问题，误判为 401）
2. Webhook 检查 → URL 空（polling 模式）
3. Gateway 日志 → provider 启动记录存在
4. Pairing 检查 → **发现关键问题**：Lee 的 ID (8051279955) 不在 paired 列表中
5. 配置检查 → **根本原因**：`memorySearch.provider: "none"` 无效值，导致 Gateway 配置加载失败，provider 未正常运行

**修复操作：**
- 将 `agents.defaults.memorySearch.provider` 从 `"none"` 改为 `"local"`
- 重启 Gateway → 所有 provider 正常启动

**教训：**
- Gateway 配置错误会导致 channel provider 无法正常工作
- `memorySearch.provider` 只接受 `["openai", "local", "gemini", "voyage", "mistral", "ollama"]`
- 配置值 `"none"` 不是有效的禁用方式

---

**状态：** 🟢 已修复
