---
agent_created: true
title: "金刚经讲读稿·语义重排编辑规范"
summary: "王德峰《金刚经》讲读整理稿的净稿与语义分段标准，覆盖OCR清理、标点修正、段落重排全流程"
created: 2026-09-01
updated: 2026-09-02
version: "1.9"
tags:
  - 文本编辑
  - 语义重排
  - 金刚经
---

# 金刚经讲读稿·语义重排编辑规范

## 项目概况

- **原始素材**：企微文档《金刚经_王德峰_讲读整理稿_上一版备份》，约120k字符，21章
- **原始章节目录**：`/tmp/jingang_chapters_raw/`（按章切分的原始长段落版）
- **输出目录**：`/Users/sanzhang/obsidian-private/outputs/金刚经_王德峰_完整字幕/上一版备份_语义重排_version_a/chapters/`
- **最终交付格式**：MD + HTML + DOCX 三种
- **全稿已交付（2026-09-02）**：20 章、638 段、**119,007 字符**，全部通过质检（段长 150–400、段尾合法、句数≥3、引号书名号配平）。合集：`金刚经讲读_王德峰_净稿全本.md`

## 硬性约束（用户 2026-09-01 下达，最高优先级）

用户原话，逐条照录，任何情况下不得违反：

> **【硬性约束】**
> 1. **绝对禁止腰斩句子**：所有换行/分段点，必须严格落在整句的末尾标点（。！？以及紧随其后的”’）之后。绝不可在逗号（，）、顿号（、）或句子中间断开。
> 2. **杜绝碎头碎脑的短句分行**（避免微信对话式分段），单段字数建议保持在 **150～400 字**之间（包含 **3～6 个完整句子**），保持段落的丰满与逻辑完整度。
> 3. **遇到人物对话、重要转折、时间/空间跨越或核心观点升华时，才设立新自然段。**

### 约束落地细则

| 约束 | 机检方式 | 违反即返工 |
|---|---|---|
| 段尾合法 | 正则 `[。！？][”’）\)]?$`，每段必须匹配 | 逗号/顿号/冒号/破折号/分号结尾 → 合并到下段或上段 |
| 段长 150–400 | `len(段)`；<150 必须并入相邻段，>400 必须拆 | 拆不动时说明，不得硬塞 |
| 段不碎 | 每段 ≥3 个完整句（防 1–2 句孤段） | 单句成段 → 合并 |
| 分段触发 | 只有「对话 / 转折 / 时空跨越 / 观点升华」四种 | 仅为"读起来长"而分段 → 重排 |

### 字数与句数冲突时的裁决（重要）

讲稿是口语体，**平均句长仅 15–20 字**。机械套用"3–6 句"会让每段只剩 60–120 字，反而违反 150 字下限。因此：

- **以字数为主要尺度**（150–400 字），句数只作**下限保护**（≥3 句，防碎段）
- 口语段 150–400 字通常对应 **5–15 句**，属正常，不算违规
- 真正要卡的是上限：单段 >400 字或 >15 句时才优先拆，拆点仍须落在整句末尾

### 可复用质检脚本（每章落盘后必跑）

```python
python3 -c '
from pathlib import Path; import re
s=Path("<章节路径>").read_text(encoding="utf-8")
ps=s.split("\n\n")[1:]          # [0] 是标题行
sent=lambda t:[x for x in re.split(r"(?<=[。！？])",t) if x.strip()]
issues=[]
for i,x in enumerate(ps,1):
    t=x.rstrip()
    if not re.search(r"[。！？][”’）\)]?$",t): issues.append(f"[{i}] 段尾非法 ...{t[-20:]}")
    if len(t)<150: issues.append(f"[{i}] 过短 {len(t)}字")
    if len(t)>400: issues.append(f"[{i}] 过长 {len(t)}字")
    if len(sent(t))<3: issues.append(f"[{i}] 句数不足")
print("段数",len(ps),"| 字符",sum(map(len,ps)),"| issues:",issues or "无")
print("弯引号",s.count("\u201c"),"/",s.count("\u201d"),"直引号",s.count(chr(34)),
      "书名号",s.count("《"),"/",s.count("》"))
print("ASCII",sorted(set(re.findall(r"[A-Za-z][A-Za-z0-9]*",s))))
'
```

ASCII 只允许出现讲者原话里的英文词（如 `devotion`、`New Concept English`）和拼音注音字母（`bōrě` 之类），其余一律是 OCR 残留，须清除。

## 编辑原则（三不改）

1. **不改原意**：忠实还原讲座口语内容，不做文学润色
2. **不删强调**：口语中的刻意重复/强调句保留（如"就是海水。这些都融会贯通了"）
3. **不删注音**：保留拼音注释，如「南无(námó)」「般若(bōrě)」「乐(yào)」

## 三步流程

### Step 1：OCR 清理 + 标点修正

- 修正 OCR 重复字（如"乐乐"→"乐"）
- 删除 OCR 噪声字符（如散落的"学"、乱码序列"M士N日"等）
- 合并被错误断开的句子
- 规范标点：中文句号/逗号/问号/叹号，书名号完整配对
- **统一中文引号**：引语一律用弯引号 `“”`，不得出现直引号 `"`。落盘后用脚本校验 `直引号数量==0` 且左右弯引号配平；若检出直引号，再按「开→闭交替」规则批量转回弯引号
- 修正明显错字（不确定的保留原文加括号标注）
- 经典引文、历史人物语句与口语残句若无音频佐证，不凭通行版本擅自改写；只处理确定的OCR噪声和语法残缺

### Step 2：语义段落重排

**核心规则：按完整语义单元分段，不按标点机械切分。**

#### 段落长度目标（用户 2026-09-01 最新硬性要求，优先于旧区间）
- **目标区间**：**150–400 字/段**，每段包含 **3–6 个完整句子**
- **严禁腰斩**：分段点必须落在整句末尾标点（。！？及紧随其后的”’）之后；逗号、顿号、句中一律不断
- **杜绝碎短段**：避免微信对话式分段，保持段落丰满与逻辑完整；<150 字的段必须并入相邻段
- **弹性**：核心论理段（如"如如之心"论述）上限可放宽至 ~550 字；密度极高的短章个别段可略低于150字，但须确认并入后不会超400字

#### 何时才另起新段（除长度超限外）
只有以下四种情形才设立新自然段：
1. **人物对话**（问答、引语角色的切换）
2. **重要转折**（语义方向反转、提出反例或设问）
3. **时间/空间跨越**（叙事场景、朝代、案例切换）
4. **核心观点升华**（从举例进入总结、从具象进入抽象结论）

#### 六条硬规则（从第一章踩坑中提炼）

| # | 规则 | 典型反例 | 正确做法 |
|---|------|----------|----------|
| 1 | **逗号不断段** | `这个心体，`¶`佛家讲的心体…` | 逗号后的内容归入下段 |
| 2 | **无孤悬片段** | `…程度。我们`¶`今天一起讨论…` | "我们"归入下段，保持句子完整 |
| 3 | **语义连续不切断** | `最尊最上最第一。`¶`一种最高的度…` | 论点→论据→结论 保持同段 |
| 4 | **同一话题讨论归一段** | "乐"字三种读音被中间切断 | 整个话题讨论归一段，在话题转换处分段 |
| 5 | **三句义完整** | 三句义论述（如来说X，即非X，是名X）被拆散 | 一组三句义及其解释保持同段 |
| 6 | **引文+解释同段** | 引用经文后立刻断段 | 经文引用与紧跟的解释放在同一段 |
| 7 | **先辨句法再合并** | `不是你去降伏，`+`那些发了菩提心的人…`直接拼成“降伏那些人” | 先判断后句主语、前句宾语和口语停顿，避免合并后改变原意 |

#### 分段锚点选择（优先级递降）

1. **话题转换**：从讨论 A 概念转向 B 概念
2. **叙事转折**：时间/人物/场景切换
3. **论证层次**：从举例转向总结、从总结转向新论点
4. **长段内的自然呼吸点**：一个完整论证链结束，下一个开始

### Step 3：通读复查

逐段检查：
- [ ] 有无孤悬片段（几个字单独成段）
- [ ] 有无逗号结尾的段落
- [ ] 有无超 550 字的段落需要拆分
- [ ] 有无语义被硬切的段落需要合并
- [ ] OCR 残留是否清干净
- [ ] **跨章用词是否与已交付章节一致**（分目题、人名、术语）
- [ ] **是否已调用 present_files 推送给用户**（易漏：处理完就去同步 Skill，结果产物从未展示）

## 大文件分批处理工作流（token/上下文受限时必用）

单章 >1.5 万字时，一次读入或一次写出都会撞上下文上限。固定套路：

1. **分批读**：`Read(..., offset=1, limit=26)` 逐段推进（约 26 行/批，视行长调整）
2. **分批写**：每批净稿写入 `/tmp/jgN_pK.md`，全部写完再 `cat /tmp/jgN_p*.md > 目标文件.md`
3. **落盘后统一引号**：分批写时为省事常用「」，合并后必须 `s.replace("「","“").replace("」","”")`
4. **合并后再质检**：`cat` 拼接处最容易产生半截句或重复标题，质检脚本重点看首尾段

## 批量切分/合并脚本（长段修复，避免手工逐处 Edit）

对「一段过长需拆」的批量修复，用累计字数定位句末，比手工找锚点快且不会切错：

```python
cuts={9:199, 23:221, 26:249}   # {1-based段号: 目标累计字数}
for i,blk in enumerate(blocks,1):
    if i in cuts:
        tgt=cuts[i]; cur=""; parts=[]; done=False
        for ch in blk:
            cur+=ch
            if (not done) and ch in "。！？”’）)" and len(cur)>=tgt:
                parts.append(cur); cur=""; done=True   # 取第一个≥target的句末
        parts.append(cur)
```

合并碎段时按「原始段号分组」批量合并，注意三件事：

- **合并后立刻复检长度**：曾把三连续段一次性合并成 708 字（超上限近一倍）。合并串要在组内逐对判断，不能一次吞掉整条链
- **合并用空字符串直接拼接**：若用 `"\n"` 拼接会在段内留软换行，与全稿「段内连写」体例不符；事后用 `s.replace("\n\n","\x00").replace("\n","").replace("\x00","\n\n")` 清理
- **英文句点要转中文句号**：段尾出现讲者原话的英文（如 `everything is okay.`）会让段尾正则失配，保留英文但把句末 `.` 改成 `。`

## 章节命名规则

- 输出文件：`{序号}_{章名}.md`（如 `00_开场.md`、`01_第二品.md`）
- 序号从 00 开始，对应原始章节顺序

## 进度追踪

| 章节 | 状态 | 段数 | 最长段 | 备注 |
|------|------|------|--------|------|
| 00_开场 | ✅ 完成 | 41 | ~522字 | 原文10110字→净稿10005字，99%保留 |
| 01_第二品 | ✅ 完成 | 35 | 542字 | 11处合并、7处拆分，结构质检通过 |
| 02_第三品 | ✅ 完成 | 10 | 431字 | OCR清理、语义重排及逐句比对完成 |
| 03_第四品 | ✅ 完成 | 10 | 356字 | 语义分段排版，无孤悬短段 |
| 04_第五品 | ✅ 完成 | 9 | 278字 | 语义分段排版，引号已统一为弯引号 |
| 05_第六品 | ✅ 完成 | 13 | 382字 | **按硬约束整章重做**（用户指出断句不佳）：151–382字，段尾100%合法；修复4处切断——腰斩句「于是有了信/越来越坚定」、顶真句「消业要被指点的/指点我们的就是佛法」、同话题「佛有四众弟子/分了在家和出家」、「佛经是在的」；"稀有"已统一为"希有" |
| 06_第七品 | ✅ 完成 | 8 | 361字 | 154–361字，内容保留率100.5%；修正"切贤圣"漏字、"说不出来的一旦"粘连 |
| 07_第八品 | ✅ 完成 | 8 | 279字 | 161–279字；补全书名号（《法华经》《阿弥陀经》缺左号、《金刚经》缺右号），繁体"解決"→"解决"；内容保留率97.9% |
| 08_第九品 | ✅ 完成 | 32 | 376字 | 156–376字，8109字；本章含「第十二点 住相与我执同」长篇与第十分引子（燃灯佛授记）；OCR 去重 10 余处（四果叙述、complex 段、授记段），「析分缕线」→「条分缕析」，「明心号菩萨」无法辨识已删；保留率97.8% |
| 09_第十品 | ✅ 完成 | 14 | 369字 | 151–369字；修正佛士→佛土、国主→国土、净士→净土、恶鬼→饿鬼，补回落字"无我相、无人相" |
| 10_第十一品 | ✅ 完成 | 9 | 355字 | 修正"不同意吧"→"同意吧"、删三连重复句；保留率98% |
| 11_第十二品 | ✅ 完成 | 3 | 267字 | 短章（原文726字）；清理"尊重正教分第"重复噪声与残缺书名号 |
| 12_第十三品 | ✅ 完成 | 47 | 327字 | **原文跨十三至十六分**；清理衾/、金灣、金博經等OCR乱码，去重三连"无众生相、无寿者相" |
| 13_第十六品 | ✅ 完成 | 15 | 347字 | 清理 `#`、𧜏𣶺 等噪点，补《资本论》《德法年鉴》《庄子》 |
| 14_第十七品 | ✅ 完成 | 47 | 397字 | **原文跨十七至十九分**（最长章之一）；统一76对直角引号，去重三连"发阿耨多罗三藐三菩提心者" |
| 15_第二十品 | ✅ 完成 | 11 | 364字 | 修正"凡圣倩忘"→"凡圣情忘"、"能所泯"→"能所俱泯"；补《宝藏论》《拿破仑法典》《历史哲学》 |
| 16_第二十一品 | ✅ 完成 | 102 | 348字 | **全稿最大章**（原文25,777字，分4批写入）；170–348字。**2026-09-02 遗漏检测补回结尾 285 字**（人性弱点/养儿防老/善不受报一段，批次接缝处漏掉）|
| 17_第二十八品 | ✅ 完成 | 20 | 370字 | 含第二十九分「威仪寂净」；153–370字 |
| 18_第三十品 | ✅ 完成 | 41 | 359字 | 含第三十一、三十二分；分2批写入；162–359字 |
| 19_第一品-法会因由分（序分与流通分） | ✅ 完成 | 10 | 320字 | 对应 raw `20_第一品`；**内容实为序分+流通分+四句偈全经总结，非第一品正文讲读，讲座放在最后讲**；"如露如甩"→"如露亦如电" |

## 章节编排逻辑（易被误判为「缺章」，务必先讲清）

本稿按**讲座实际讲授顺序**编号，不是按《金刚经》三十二分的经文顺序。三个反直觉之处：

1. **第一品排在最后（`19_`），不是最前。** 讲座开场后直接从第二品讲起；第一品的开经段（「一时，佛在舍卫国……次第乞已，还至本处」）与流通分、四句偈一起放在收尾处讲，承担全经总结功能。所以 `00_开场` 之后直接接 `01_第二品` 是正常的，**不是漏处理**。文件名须保留「第一品」字样（`19_第一品-法会因由分（序分与流通分）.md`），否则读者会以为缺章。
2. **一章可跨多个经文分目**：第十三品章含十三至十六分；第十七品章含十七至十九分；第三十品章含三十、三十一、三十二分；第二十八品章含二十八、二十九分。
3. **经文分目有跳讲**：讲座未逐分讲读，第十四、十五分并入第十三品章，第十八、十九分并入第十七品章，第二十二至二十七分并入第二十一品章。

**结论：20 个输出文件覆盖全部 21 个 raw 文件（raw 00 为总稿头说明，不单列）。章节数对不上经文品数是内容本身决定的，不是遗漏。**

### 全稿回修（2026-09-02）

早期四章（00–03）在统一质检时暴露体例不一致，已回修至与新章同标准：

| 章节 | 回修动作 | 结果 |
|---|---|---|
| 00_开场 | 5处长段按句末切分（482/478/522/415/415字）、4组碎段合并（含28字与65字段）、删除编辑注记「（原文句式整理）」、英文句点 `everything is okay.`→`。` | 41段→38段，156–398字 |
| 01_第二品 | 5处长段切分（421/542/480/514/485字）、1处注记删除 | 35段→40段，195–376字 |
| 02_第三品 | 段7(431)切分、段4+5合并后重新二分（避免合过头成708字）、段9+10合并 | 10段，178–393字 |
| 03_第四品 | 段1(121字)并入举例段后，在例子内部重新二分 | 10段，192–356字 |

## 常见陷阱

- **错误粘连改变句法**：原稿的逗号或换行可能代表口语停顿，不能只删除分隔符机械拼接；须先判断主谓宾关系。
- **用通行版本覆盖讲者原话**：典故、经文或名言疑似有误时，若没有音频依据，不直接替换成记忆中的标准表述。
- **段落过碎但形式合规**：即使每段都超过150字，也要复查“例证—解释—结论”是否被拆散；段长合格不等于语义完整。
- **讲者的文字游戏被段落边界切断**：王德峰常做字词拆解式修辞（如把“所知障”翻转说成“知是种障”、“圣贤”倒成“贤圣”）。这类拆解的前后两句必须同段，否则读者看不出修辞意图。
- **对偶/排比句被拆两段**：如“也不在佛法上着相”与“也不在世法上着相”，是成对出现的，必须同段。
- **顶真/接力句被段落边界切断**：讲者常用上一句末尾的词作下一句开头（如“消业要被指点的。指点我们的就是佛法。”“于是有了信，越来越坚定。”）。**顶真的两句必须同段**，否则衔接词悬空、气势断裂。修法：不是把两句硬拼，而是把分段点移到该论证单元的更早处。
- **同话题被段界劈成两半**：连续的背景交代（如“合起来叫四众。佛有四众弟子。”与“分了在家和出家，分了男性与女性”）不可断开；判断标准是下段首句是否能独立成立，不能独立就必须并回上段。
- **为凑段长而牺牲语义完整**：段长合格 ≠ 语义完整。每章落盘后必须逐对检查「上段末句 + 下段首句」，确认不是同一句话、同一修辞或同一话题被切开。
- **跨章专有名词不统一**：佛经分目题、人名、术语在不同章用了不同写法（如“希有”/“稀有”）。每完成一章要回头比对已交付章节，统一为原文章题的写法。
- **编辑注记残留在正文**：早期整理时留下的括注（如「（原文句式整理）」「（原文含乱码，按义整理）」「（原文"……"）」）不是讲者原话，会同时造成段尾标点失配与内容污染。**注记不一定只在段尾，也可能嵌在段落中间**（01_第二品 一次查出 7 处，多数在段中）。必须全稿跑正则扫描，不能只靠段尾检查：

  ```python
  for m in re.finditer(r'[（(][^（()）]{2,40}[)）]', s):
      if any(k in m.group() for k in ['原文','乱码','按义','依义','依语音','依上下文','整理']):
          s = s.replace(m.group(), "", 1)
  ```

  删完后要重跑质检——注记少的那些段落可能因此跌破 150 字下限。
- **分批写入时批次边界漏内容（曾造成 272 字整段丢失）**：长章拆成 partA/partB 多批写入后 `cat` 合并，**批次接缝处最容易丢内容**。第二十一品分 4 批写入，最后一批的边界处理漏掉了结尾 285 字的完整发挥段，靠保留率（96.9%，看着正常）完全没发现——**保留率无法证明没有整句丢失**。必须补一道遗漏检测，见下方「遗漏检测脚本」。
- **为消灭短段而合并过头**：把 3 个连续短段一次吞并，很容易造出 700 字的超长段。正确做法是逐对试合并，每次合并后立刻量长度；或用「先合并、再在新段内部重新二分」的方式两头逼近区间。
- **切分点选在"所以/那么"之前**：长段切分时若切点落在「所以……」「那么……」这类承接词之前，新段会以悬空连词开头。优先把切点选在**总结句之后、承接句之前**，让承接词留在新段段首。
- **设问与作答被段界切开**：讲者口语体大量使用自问自答（如“那么这个常在哪里？”→ 下段“当然，人类社会…会发明出常来的”）。**设问句与其作答必须同段**。修法：若两段的字数之和会突破 400 字上限，就把设问句整体移到作答段开头（而不是把两段硬拼），上段在设问前收尾。
- **原始章节文件可能跨界**：讲读稿不是严格按经文分品切分的——一章原文里可能夹着讲座的分点编号（如“我们讲第十二点：住相与我执同”）和下一品的引子（如第九品原文末尾整段讲燃灯佛授记，而授记正文在第十品）。处理前先通读全章确认内部板块，按板块分段，不要按原始行号机械切。

## 遗漏检测脚本（保留率之外必做的一步）

**为什么需要**：保留率 96.9% 看起来正常，但曾有一个 272 字的完整段落被整段漏掉。保留率只反映总量，无法证明没有整句丢失。

**做法**：把原文与净稿都做归一化（去标题行、去所有空白与标点、去数字与 ASCII），用 `difflib.SequenceMatcher` 取 `delete`/`replace` 块，再算每个块在净稿中的**最长公共子串覆盖率**——覆盖率低才是真遗漏，覆盖率高说明净稿保留了核心、只是改了字（OCR 纠错）。

```python
import difflib
def norm(s):
    s = re.sub(r"^#+[^\n]*\n", "", s, count=1)
    return "".join(c for c in s if c not in NOISE)   # NOISE = 标点+空白+数字+ASCII
def lcs(a, b):            # 最长公共子串长度
    m = [[0]*(len(b)+1) for _ in range(len(a)+1)]; best = 0
    for i in range(1, len(a)+1):
        for j in range(1, len(b)+1):
            if a[i-1] == b[j-1]:
                m[i][j] = m[i-1][j-1] + 1
                best = max(best, m[i][j])
    return best
for tag,i1,i2,j1,j2 in difflib.SequenceMatcher(None, norm(raw), norm(clean), autojunk=False).get_opcodes():
    if tag in ("delete","replace"):
        seg = norm(raw)[i1:i2]
        if len(seg) >= 12 and lcs(seg, norm(clean))/len(seg) < 0.5:
            print("疑似遗漏", len(seg), seg[:80])
```

**判定经验**：覆盖率 <50% 且长度 ≥12 字的块要人工看。但其中**绝大多数是 OCR 三连重复**（如「庄严佛士……庄严佛士……庄严佛士」）或乱码，净稿已保留一遍，属正确删除。真正的遗漏往往表现为：**长度异常突出**（其他块都在 12–35 字，真遗漏是 272 字）、**内容语义完整**（有论点有例子，如「人性有许多共同的弱点……养儿防老……善不受报」）、**关键词在净稿中搜不到**（用原文 5 字关键词 grep 净稿验证）。

## 注意事项

- **全稿一致性优先于单章**：新章达标不等于全稿达标。早批章节（00–03）在统一质检时暴露出长段未拆、碎段未并、编辑注记残留三类问题。**每完成一批新章，就回头跑一次全稿质检**，不要等全部做完
- 用户已明确「不用每次都确认，自己做好校验即可」——可自主连续推进多章，但每章落盘后质检脚本必跑、issues 必清
- 每章处理完后推送给用户看，确认后再继续（批量推进时改为每批完成后统一推送）
- 段落调整用 Edit 工具逐处修改，不要整文件重写（保留 git diff 可追踪性）
- 用户可能反馈个别段落不合理，按反馈微调即可
